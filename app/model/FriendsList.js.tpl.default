Ext.define('{appName}.model.FriendsList', {
    extend: '{appName}.model.Base',

    fields: [
        {
            name: 'friendsName'
        },
        {
            name: 'connectionStatus'
        }
    ]
});
